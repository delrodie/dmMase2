{% extends 'base.html.twig' %}

{% block title %}Instance{% endblock %}

{% block body %}
    <h1>Instance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instance.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ instance.titre }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ instance.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_instance_index') }}">back to list</a>

    <a href="{{ path('app_backend_instance_edit', {'id': instance.id}) }}">edit</a>

    {{ include('backend_instance/_delete_form.html.twig') }}
{% endblock %}
