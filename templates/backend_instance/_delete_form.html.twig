<form method="post" action="{{ path('app_backend_instance_delete', {'id': instance.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette instance?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instance.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
