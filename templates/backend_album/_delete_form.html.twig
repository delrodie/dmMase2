<form method="post" action="{{ path('app_backend_album_delete', {'id': album.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet album?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ album.id) }}">
    <button class="btn btn-danger btn-lg">Supprimer l'album</button>
</form>
