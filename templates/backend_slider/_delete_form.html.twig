<form method="post" action="{{ path('app_backend_slider_delete', {'id': slider.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce slide?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ slider.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
